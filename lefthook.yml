pre-commit:
  commands:
    format:
      run: pnpm format
      stage_fixed: true

pre-push:
  commands:
    test:
      run: pnpm test
    typecheck:
      run: pnpm typecheck
    build:
      run: pnpm build
